import { ProducemodePipe } from './producemode.pipe';

describe('ProducemodePipe', () => {
  it('create an instance', () => {
    const pipe = new ProducemodePipe();
    expect(pipe).toBeTruthy();
  });
});
