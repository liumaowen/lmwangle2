import { SalebillstatusPipe } from './salebillstatus.pipe';

describe('SalebillstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SalebillstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
