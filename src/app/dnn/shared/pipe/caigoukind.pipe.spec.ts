import { CaigoukindPipe } from './caigoukind.pipe';

describe('CaigoukindPipe', () => {
  it('create an instance', () => {
    const pipe = new CaigoukindPipe();
    expect(pipe).toBeTruthy();
  });
});
