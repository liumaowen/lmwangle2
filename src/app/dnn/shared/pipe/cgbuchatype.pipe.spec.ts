import { CgbuchatypePipe } from './cgbuchatype.pipe';

describe('CgbuchatypePipe', () => {
  it('create an instance', () => {
    const pipe = new CgbuchatypePipe();
    expect(pipe).toBeTruthy();
  });
});
