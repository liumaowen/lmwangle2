import { OrderpaytypePipe } from './orderpaytype.pipe';

describe('OrderpaytypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderpaytypePipe();
    expect(pipe).toBeTruthy();
  });
});
