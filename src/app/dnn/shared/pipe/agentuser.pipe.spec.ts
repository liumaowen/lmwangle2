import { AgentuserPipe } from './agentuser.pipe';

describe('AgentuserPipe', () => {
  it('create an instance', () => {
    const pipe = new AgentuserPipe();
    expect(pipe).toBeTruthy();
  });
});
