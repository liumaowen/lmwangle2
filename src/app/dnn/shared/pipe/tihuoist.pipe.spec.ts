import { TihuoistPipe } from './tihuoist.pipe';

describe('TihuoistPipe', () => {
  it('create an instance', () => {
    const pipe = new TihuoistPipe();
    expect(pipe).toBeTruthy();
  });
});
