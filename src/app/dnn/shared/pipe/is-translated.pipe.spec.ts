import { IsTranslatedPipe } from './is-translated.pipe';

describe('IsTranslatedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsTranslatedPipe();
    expect(pipe).toBeTruthy();
  });
});
