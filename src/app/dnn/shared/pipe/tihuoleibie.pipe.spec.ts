import { TihuoleibiePipe } from './tihuoleibie.pipe';

describe('TihuoleibiePipe', () => {
  it('create an instance', () => {
    const pipe = new TihuoleibiePipe();
    expect(pipe).toBeTruthy();
  });
});
