import { OrderisonlinePipe } from './orderisonline.pipe';

describe('OrderisonlinePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderisonlinePipe();
    expect(pipe).toBeTruthy();
  });
});
