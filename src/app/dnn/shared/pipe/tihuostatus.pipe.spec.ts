import { TihuostatusPipe } from './tihuostatus.pipe';

describe('TihuostatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TihuostatusPipe();
    expect(pipe).toBeTruthy();
  });
});
