import { SubmitverifyPipe } from './submitverify.pipe';

describe('SubmitverifyPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmitverifyPipe();
    expect(pipe).toBeTruthy();
  });
});
