import { SalebillmailPipe } from './salebillmail.pipe';

describe('SalebillmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SalebillmailPipe();
    expect(pipe).toBeTruthy();
  });
});
