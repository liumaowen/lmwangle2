import { Ordertype1Pipe } from './ordertype1.pipe';

describe('Ordertype1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Ordertype1Pipe();
    expect(pipe).toBeTruthy();
  });
});
