import { JsbuchatypePipe } from './jsbuchatype.pipe';

describe('JsbuchatypePipe', () => {
  it('create an instance', () => {
    const pipe = new JsbuchatypePipe();
    expect(pipe).toBeTruthy();
  });
});
