import { TihuotypePipe } from './tihuotype.pipe';

describe('TihuotypePipe', () => {
  it('create an instance', () => {
    const pipe = new TihuotypePipe();
    expect(pipe).toBeTruthy();
  });
});
