import { OrdertypePipe } from './ordertype.pipe';

describe('OrdertypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdertypePipe();
    expect(pipe).toBeTruthy();
  });
});
