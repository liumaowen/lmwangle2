import { CaigoutypePipe } from './caigoutype.pipe';

describe('CaigoutypePipe', () => {
  it('create an instance', () => {
    const pipe = new CaigoutypePipe();
    expect(pipe).toBeTruthy();
  });
});
