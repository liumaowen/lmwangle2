import { TransporttypePipe } from './transporttype.pipe';

describe('TransporttypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransporttypePipe();
    expect(pipe).toBeTruthy();
  });
});
