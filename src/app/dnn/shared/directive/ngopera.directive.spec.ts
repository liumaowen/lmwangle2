import { NgoperaDirective } from './ngopera.directive';

describe('NgoperaDirective', () => {
  it('should create an instance', () => {
    const directive = new NgoperaDirective();
    expect(directive).toBeTruthy();
  });
});
